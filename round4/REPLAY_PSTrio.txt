PSTrio ROUND 4 REPLAY — GEMINI AGENT RUN

TEST_SITE_URLS: https://www.adobe.com/ (replace this URL if the submitted video audits another site)
AGENT_HARNESS: Gemini CLI, interactive session. Record the version printed by gemini --version on the recording computer.
MODEL: Gemini CLI's model routing for the signed-in personal Google account. Record the model displayed during the submitted run.
ENGINE: The unchanged Round 3 submission.zip. SHA-256: 1d5e0f3e4dd12a8c12e205b4723124b4d441cdbf3f76605a2befde9114ec33b1.
ENV_SETUP: Windows PowerShell, Python 3, Node.js 20+, Gemini CLI installed with npm, personal Google sign-in, outbound HTTPS. No Python packages are needed. Free Gemini CLI usage is subject to Google's current quota.

TEST_STEPS:
 1. Extract PSTrio_video_demo.zip. Open PowerShell in the extracted folder.
 2. If needed, install Gemini CLI with: npm install -g @google/gemini-cli
    Start gemini once and choose Sign in with Google. Complete the browser sign-in.
 3. Check the frozen package and create a fresh workspace:
      Get-FileHash .\submission.zip -Algorithm SHA256
      python round4/setup_gemini_demo.py
    The setup script checks the full expected hash, then installs eight byte-identical skills at demo/gemini_run/.gemini/skills/ and creates demo/gemini_run/audit_out/.
 4. Enter the workspace and start Gemini:
      cd demo/gemini_run
      gemini
    Trust the workspace if asked. Run /skills list and confirm audit-orchestrator appears.
 5. Paste this prompt. Change only the URL if the video uses another site:
      Activate the audit-orchestrator skill and audit https://www.adobe.com/ for AI discoverability and on-site engagement. Use today's real date and sample at most 5 pages. On Windows use python for the skill scripts if python3 is unavailable. Write every generated file under ./audit_out/, ending with audit_out/audit_report.json and audit_out/audit_report.md. Perform the skill's off-site web-search corroboration and homepage orientation checks, run the evidence critic, and review surviving findings before finalizing. Leave .gemini/skills unchanged. Summarize the critical and high findings and the remediation roadmap.
 6. Approve skill activation and the Python script calls. The agent should collect evidence once, run six analyzers, use web search for off-site corroboration, assess homepage orientation, run the evidence critic, review surviving findings, and finalize the JSON and Markdown reports. If a step cannot run, the report or final message must identify the gap; do not invent evidence.
 7. From another PowerShell tab in the extracted folder, confirm:
      Test-Path demo/gemini_run/audit_out/audit_report.json
    A completed run prints True. If False, inspect the Gemini error and start a new workspace for the next take.
 8. Open that run's output in the GUI from the extracted folder:
      python gui/server.py --engine-only --open demo/gemini_run/audit_out
    The GUI is an imported-report viewer here. Its engine launcher did not produce the recorded agent report.

EXPECTED_OUTPUT: demo/gemini_run/audit_out/audit_report.json and audit_report.md, alongside evidence.json, raw_findings.json, and adjudicated.json. Report counts, severities, and score depend on the live site and the agent's review; read them from the submitted run. The GUI displays those same files.
REPLAY_LIMIT: The crawl samples at most five pages. Off-site search and agent judgment can vary. No fixed score or finding count is promised.
