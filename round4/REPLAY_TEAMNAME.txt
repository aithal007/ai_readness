TEST_SITE_URLS: https://www.sqlite.org/
LLM_MODEL: Claude Sonnet 5 (model id claude-sonnet-5), run on 2026-09-22
AGENT_HARNESS: Claude Code v2.1.278
ENV_SETUP: No pip install and no requirements.txt. The marketplace is Python 3 standard library only (tested on Python 3.14.7). Needs outbound HTTPS, and the harness must have its WebSearch tool enabled for the off-site corroboration step.
TEST_STEPS:
 1. From an empty directory containing the Round 3 archive (named final_adobe here; any name works), install the submitted skills where Claude Code discovers them and prove they are byte-identical to the submitted package. Expected last line: ENGINE_IDENTICAL.
      python3 -m zipfile -e final_adobe r3 && mkdir -p demo/.claude/skills && cp -r r3/brand-ai-readiness-audit/skills/. demo/.claude/skills/ && diff -r r3/brand-ai-readiness-audit/skills demo/.claude/skills && echo ENGINE_IDENTICAL && cd demo
 2. Start the harness in that directory. The Skill permission is required: without it Claude Code silently refuses to load the entrypoint skill.
      claude --model claude-sonnet-5 --allowedTools Skill "Bash(python3:*)" "Bash(python:*)" "Bash(mkdir:*)" Read Write WebSearch
 3. Give the agent this prompt, verbatim:
      Use the audit-orchestrator skill to audit https://www.sqlite.org/ for AI discoverability and on-site engagement. Use today's real date. Write every output file into ./audit_out/ (create it), ending with audit_out/audit_report.json and audit_out/audit_report.md. Then summarise the critical and high findings and the remediation roadmap.
 4. The agent invokes the audit-orchestrator entrypoint, which crawls the site once into audit_out/evidence.json, runs the six analyzers, does two judgement checks itself (off-site corroboration by web search, homepage orientation), adjudicates every finding, then finalizes. Expect roughly 6 minutes of wall-clock time. The report lands in audit_out/audit_report.json (machine-readable) and audit_out/audit_report.md (readable). Each finding carries id, title, severity, evidence, suggested_action, a stable code, a confidence, and an effort; the report also carries a roadmap that buckets fixes into now, next and later by impact against effort.
 5. Expected substance, stable across two independent rehearsal runs on the same harness and model (wording, finding count and score vary a little between runs: 12 to 16 findings, readiness 94 to 96): zero critical and zero high findings. Medium: no XML sitemap (sitemap.xml returns 404 and robots.txt has no Sitemap line), and the homepage has no H1 (its headings are all H3). Low: GPTBot and ClaudeBot are refused with HTTP 403 while search and live-fetch agents (OAI-SearchBot, ChatGPT-User, Claude-SearchBot, Claude-User, PerplexityBot) get HTTP 200, which the audit reports as a deliberate training-crawler policy rather than a citation defect; no structured data on any sampled page (medium in one rehearsal, low in the other); two reference pages carry 2023 and 2024 last-updated dates; most pages have no meta description; no lang attribute on the html element. All of these can be confirmed independently with curl.
 6. FALLBACK if Claude Code is not available. This runs the same engine with no LLM. It produces every scripted finding but NOT the two judgement checks or the critic review, so its substance differs: it reports the 403 on GPTBot and ClaudeBot as a critical network-layer block (readiness 70, 1 critical, 6 high, 6 medium, 8 low). Any other agent harness that can read a SKILL.md and run shell commands should instead follow demo/.claude/skills/audit-orchestrator/SKILL.md, which is provider-neutral and reproduces the agent-reviewed result above.
      mkdir -p audit_out && S=.claude/skills && TODAY=$(date +%F) && python3 $S/audit-orchestrator/scripts/run_audit.py https://www.sqlite.org/ --out audit_out/raw_findings.json --evidence-out audit_out/evidence.json --today $TODAY && python3 $S/evidence-critic/scripts/critique_findings.py --findings audit_out/raw_findings.json --evidence audit_out/evidence.json --out audit_out/adjudicated.json && python3 $S/audit-orchestrator/scripts/finalize_report.py audit_out/adjudicated.json --site www.sqlite.org --evidence audit_out/evidence.json --out audit_out/audit_report.json --md audit_out/audit_report.md
EXPECTED_OUTPUT: audit_out/audit_report.json
